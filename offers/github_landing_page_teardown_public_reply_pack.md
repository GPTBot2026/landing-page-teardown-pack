# GitHub Landing Page Teardown Public Reply Pack

Purpose: make public founder-thread outreach usable immediately after publication without sounding like spam or forcing custom drafting.

Use after the GitHub repo and Pages site are live. Replace:

- `[PAGES_URL]`
- `[REPO_URL]`
- `[ISSUE_URL]`

Rule:
- Only use these where feedback is clearly welcome or where the founder has already raised a landing-page problem.
- Do not post in product support issues, bug reports, or unrelated announcement threads.
- Keep the reply useful on its own. The link is secondary.

## 1. "My landing page is not converting"

```md
If you want a low-friction second read on it, I run a USD 20 AI-assisted teardown for one public landing page.

The flow is lightweight: short free preview first, then the full pack only if the preview is useful.

Details: [PAGES_URL]
```

## 2. "I need copy feedback"

```md
This is a fit for a small teardown rather than a big consulting project.

I review one public page, post a short preview first, and only then offer the full USD 20 pack if it looks useful.

Offer page: [PAGES_URL]
```

## 3. "Does anyone have feedback on this homepage?"

```md
If you want, I can point to the first thing I would test.

I run a low-ticket landing page teardown for one public page. The request path is here: [ISSUE_URL]
```

## 4. "We are struggling to explain what this product does"

```md
That is exactly the kind of problem this teardown is built for.

It stays narrow: one public page, AI-assisted text feedback, short free preview first, then USD 20 only if you want the full review.

Info: [PAGES_URL]
```

## 5. "Roast my landing page"

```md
If you want a structured version instead of a random roast, I run a USD 20 teardown pack for one public landing page.

You get a short preview first, then the full pack only if the preview is useful.

Repo and request flow: [REPO_URL]
```

## 6. "Any advice on our CTA/headline?"

```md
If the page is public, I can give a short preview of the first CTA or headline fix I would test.

The lightweight request path is here: [ISSUE_URL]
```

## 7. Public Follow-Up After A Founder Replies Positively

```md
If you want the lightest next step, drop the page URL into the request flow and I can show the first issue I would fix before you buy anything.

Request path: [ISSUE_URL]
```

## 8. Public Follow-Up After Someone Asks "What Do You Include?"

```md
The full USD 20 pack includes:

- ranked conversion problems
- three replacement headlines
- one CTA rewrite
- the first fix to ship

Full details: [PAGES_URL]
```

## 9. Public Follow-Up After A Generic "Send Link"

```md
Everything is here: [PAGES_URL]

If it fits, open a request issue with one public page URL and I will post a short preview first.
```
